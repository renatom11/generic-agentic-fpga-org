name: journal-check

# Re-verifies the journal/commit protocol (agents/PROTOCOL.md §5). Runs the
# check over the FULL history every time — an incremental range check cannot
# detect a rewrite of already-pushed history (AUD-0001-F3) — and then over the
# pushed range for a precise failure message.
# This workflow is intentionally toolchain-free; the build/test CI arrives in M1.

on:
  push:
  pull_request:

jobs:
  journal-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Protocol enforcement self-test
        run: bash scripts/test_protocol.sh

      - name: Verify full history (append-only cannot be re-checked incrementally)
        run: bash scripts/check_journals.sh --all

      - name: Verify commit range
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          BEFORE_SHA: ${{ github.event.before }}
          AFTER_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = "pull_request" ]; then
            RANGE="$BASE_SHA..$AFTER_SHA"
          elif [ -n "$BEFORE_SHA" ] \
              && [ "$BEFORE_SHA" != "0000000000000000000000000000000000000000" ] \
              && git cat-file -e "$BEFORE_SHA" 2>/dev/null; then
            RANGE="$BEFORE_SHA..$AFTER_SHA"
          else
            RANGE=""
          fi
          if [ -n "$RANGE" ]; then
            bash scripts/check_journals.sh --range "$RANGE"
          else
            bash scripts/check_journals.sh --all
          fi
